# Top-level .env consumed by docker-compose.yml and docker-compose.prod.yml.
# Copy to .env and adjust before `docker compose up`.

# --- Postgres ---
POSTGRES_USER=tracker
POSTGRES_PASSWORD=tracker
POSTGRES_DB=tracker

# --- Backend (overrides defaults inside the container) ---
LOG_LEVEL=info
CORS_ORIGINS=http://localhost:5173

# Generate strong secrets for prod: openssl rand -base64 48
JWT_ACCESS_SECRET=change_me_access
JWT_REFRESH_SECRET=change_me_refresh
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

THROTTLE_TTL=60
THROTTLE_LIMIT=120

# --- Frontend build arg ---
# Where the browser will reach the API. In prod, this is your domain (https://example.com/api).
VITE_API_URL=http://localhost:3000/api

# --- Production (docker-compose.prod.yml only) ---
# Docker Hub namespace where images are pushed by CI.
DOCKERHUB_USER=your-dockerhub-username
# Tag to deploy. CI typically pushes both :latest and :<git-sha>.
IMAGE_TAG=latest
