backend/dist/
backend/build/
backend/node_modules/
backend/.env
backend/*.log
backend/.vscode/
backend/.idea/
node_modules/

# Local env files. Only .env.example is committed; copy it to .env for local dev.
.env
.env.local
.env.*.local
frontend/.env
frontend/.env.local

# Local load-testing stack (k6 scripts, compose, results) — lives only on the
# load generator machine, never committed.
/loadtest/

# Editor/OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
